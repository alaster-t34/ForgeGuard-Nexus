# ForgeGuard Nexus 5.0 — 0.7.1 Release Notes

## 修复内容

1. 修复运行总览中“最近工业事件 → 全部”点击无响应：改为独立事件导航动作，不再依赖容易冲突的通用导航属性或内联脚本。
2. 修复“工单执行”无法切换不同工单：详情区现在严格跟随用户选中的事件/工单，左侧选中态和 `aria-pressed` 同步更新。
3. 修复“能耗与环境”“边缘设备”“系统审计”错误回到运行总览：根因是导航配置数组中 view 与 icon 的顺序写反，现已统一为 `[view, icon, 中文名, 说明]`。
4. 前端静态资源缓存版本提升至 `0.7.1`，避免浏览器继续加载旧脚本。

## 回归验证

测试流程：

- 运行总览 → 最近工业事件 → 全部 → 告警事件页面；
- 工单执行 → 在两张工单之间切换 → 详情、选中态同步更新；
- 侧栏 → 能耗与环境 / 边缘设备 / 系统审计 → 各自独立页面；
- 检查控制台错误、页面运行错误和错误回退到运行总览。

结果：全部通过。详见 `docs/QA_REPORT.md`。
