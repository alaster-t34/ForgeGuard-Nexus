# ForgeGuard Nexus 5.0 — 0.7.0 Release Notes

## 版本目标

0.7.0 针对真实使用反馈修复两个不可用交互，扩展工业场景范围，并将 Windows 启动方式改为独立桌面软件窗口。

## 主要变化

### 全局搜索修复

- 搜索弹层移动到独立顶层容器，避免被应用布局和层叠上下文阻断；
- 增加直接 UI 调用回退；
- 支持鼠标、Ctrl+K、方向键、Enter 和 Esc；
- 搜索范围新增 25 个工业场景；
- HTML 对 JavaScript 和 CSS 使用 `?v=0.7.0`，避免旧缓存继续加载失效脚本。

### 最近工业事件“全部”修复

- 修复错误的导航目标；
- 增加直接导航回退；
- 无事件时进入告警事件空状态，不再表现为点击无反应。

### 工业场景库

新增独立“工业场景库”页面与 25 个受治理演示场景，覆盖：

- 轴承与旋转机械；
- 齿轮箱与传动；
- 泵风机与流体设备；
- 电机与电气；
- 能源与环境；
- 数据质量与治理；
- 生产与韧性。

场景可搜索、分类筛选、选择和直接运行。场景结果明确标记为模拟演示，不替代实时检测和 CSV 分析。

### 功能入口扩展

运行总览增加常用功能中心：

- 实时检测；
- CSV 文件分析；
- 工业场景库；
- 告警事件中心；
- 维修决策竞技场；
- 导出运行摘要。

### 可读性

- 证据标题、证据源、智能体动作和执行理由改为中文任务语言；
- 治理规则、诊断标题和 Agent 页面眉题中文化；
- 保留必要的工业缩写，如 RMS、RUL、FMEA，并在说明书中解释。

### Windows 桌面软件

- `ForgeGuard-Nexus.exe` 默认使用 Edge/Chrome App Mode；
- 独立最大化窗口；
- 无地址栏和普通浏览器标签页；
- 独立软件配置目录；
- 后端健康检查通过后才打开界面；
- 提供 `ForgeGuard-Nexus.exe web` 普通浏览器调试模式；
- 提供 `ForgeGuard-Nexus-Stop.exe` 停止后台服务。

## 兼容性

- Windows 10/11 x64；
- Arch Linux x86-64；
- Debian/Ubuntu x86-64 与 ARM64；
- NVIDIA Jetson Orin / JetPack 6.2.x。

## 真实边界

- Windows EXE 是桌面启动器和 App Mode 容器，不是捆绑全部 Python、模型、数据库和浏览器内核的单文件绿色软件；
- 25 个场景是软件验证与竞赛演示数据，不是物理实验；
- 默认 HGB 基线仍不能宣传为 TensorRT 神经网络；
- 工业现场使用前仍需安全、网络、身份权限和设备连接器验收。
