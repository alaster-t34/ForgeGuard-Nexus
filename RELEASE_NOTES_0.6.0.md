# ForgeGuard Nexus 5.0 — 0.6.0 Release Notes

本版本针对实际试用反馈完成四项产品级修订。

## 1. 全局搜索可用

- 右上角搜索框可点击打开；
- 支持 `Ctrl+K`；
- 支持关键字过滤、方向键选择、Enter 打开、Esc 关闭；
- 可搜索页面、设备、告警、工单及“实时检测”“CSV 分析”等帮助主题。

## 2. 同时支持实时检测和 CSV 文件分析

### 实时检测

- 创建、停止实时检测会话；
- 内置连续演示信号；
- 外部 HTTP 数据帧接口；
- 串口与 CSV 实时回放边缘客户端；
- 实时波形、质量门控、故障判断、置信度、异常概率与最近帧记录；
- 可在异常时进入告警与维修闭环。

### CSV 分析

- 支持单列/多列、有表头/无表头；
- 支持 UTF-8、GB18030、UTF-16；
- 支持逗号、分号、Tab、竖线分隔；
- 自动识别信号列或手工指定列名/序号；
- 窗口化质量检查和故障分析；
- 保存窗口级报告，可选生成告警。

两种入口使用同一套数据质量、特征与模型处理链。

## 3. Windows x64 EXE

新增：

- `ForgeGuard-Nexus.exe`：启动软件并打开浏览器；
- `ForgeGuard-Nexus-Stop.exe`：停止软件；
- Go 源码与 GitHub Actions 可复现构建流程。

EXE 是真实 Windows PE 启动器，不虚假声称为包含 Python、数据库和全部模型的单文件离线安装包。

## 4. 可读性和说明书

- 菜单、模块标题和 Agent 名称改为清晰中文；
- 新增内置普通语言帮助；
- 修复实时/CSV 波形的错误黑色填充；
- 根目录 README 重写为真实能力、操作步骤、数据格式、平台部署和边界说明；
- 新增面向普通使用者的任务式说明书。

## 兼容性

- 后端及 Windows/Docker：Python 3.11；
- Jetson/边缘客户端：保持 Python 3.10 兼容；
- JetPack 6.2.2 / L4T R36.5 / TensorRT 10.3 路径保留；
- Windows 10/11 x64、Arch Linux、Debian/Ubuntu、Jetson Orin。
