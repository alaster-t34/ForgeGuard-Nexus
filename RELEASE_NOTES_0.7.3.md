# ForgeGuard Nexus 5.0 v0.7.3

## 完整软件包重构版

本版本不提供、也不依赖热修复包。所有前端与交互修复均已直接合并到完整软件工程中。

### 已集成

- AI 智能体工作台：智能体卡片、流程阶段、职责详情、执行记录、工具权限和模型目录均可操作。
- “最近工业事件 → 全部”导航修复。
- 工单列表可切换不同工单，并同步更新右侧详情。
- “能耗与环境”“边缘设备”“系统审计”均使用独立页面，不再回退到运行总览。
- 全局搜索、工业场景库、实时检测、CSV 分析与 25 个工业场景。
- Windows 独立 App Mode 启动器、Linux/Arch/Debian 与 Jetson 部署脚本。

### 发布原则

- 用户只需下载完整跨平台包或 Windows x64 完整包。
- 不需要先安装旧版，也不需要执行补丁脚本。
- 升级时应停止旧服务、备份旧目录、解压新包并重新启动。
